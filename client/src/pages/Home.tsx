import { MetricCard, PageHeader, StatusPill, money, dateLabel } from "@/components/crm/ui";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { trpc } from "@/lib/trpc";
import { BadgeDollarSign, CalendarCheck2, Download, FileCheck2, Filter, Target, TriangleAlert, UsersRound } from "lucide-react";
import { useMemo, useState } from "react";
import { Bar, BarChart, CartesianGrid, Legend, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";

const stageLabels: Record<string, string> = { new: "Novas", qualified: "Qualificadas", proposal: "Proposta", negotiation: "Negociação", won: "Ganhos", lost: "Perdidos" };
const currencyAxis = (value: number) => new Intl.NumberFormat("pt-BR", { notation: "compact", style: "currency", currency: "BRL", maximumFractionDigits: 0 }).format(value);
const iso = (date: Date) => date.toISOString().slice(0, 10);
const currentMonthStart = () => iso(new Date(new Date().getFullYear(), new Date().getMonth(), 1));
const currentMonthEnd = () => iso(new Date(new Date().getFullYear(), new Date().getMonth() + 1, 0));
const readableDate = (value: string) => new Date(`${value}T12:00:00`).toLocaleDateString("pt-BR");

export default function Home() {
  const [startDate, setStartDate] = useState(currentMonthStart); const [endDate, setEndDate] = useState(currentMonthEnd); const [seller, setSeller] = useState("all");
  const chartInput = useMemo(() => ({ startDate, endDate, sellerId: seller === "all" ? undefined : Number(seller) }), [startDate, endDate, seller]);
  const queryOptions = { refetchInterval: 30_000, refetchOnWindowFocus: true };
  const customers = trpc.customers.list.useQuery(undefined, queryOptions);
  const contracts = trpc.contracts.list.useQuery(undefined, queryOptions);
  const pipeline = trpc.sales.pipeline.useQuery(undefined, queryOptions);
  const summary = trpc.dashboard.summary.useQuery(undefined, queryOptions);
  const charts = trpc.dashboard.commercialCharts.useQuery(chartInput, queryOptions);
  const loading = customers.isLoading || contracts.isLoading || pipeline.isLoading || summary.isLoading;
  const funnelData = charts.data?.funnel.filter(item => item.stage !== "lost").map(item => ({ ...item, label: stageLabels[item.stage] })) ?? [];
  const goalData = charts.data?.goals.map(item => ({ ...item, seller: item.sellerName.length > 16 ? `${item.sellerName.slice(0, 16)}…` : item.sellerName })) ?? [];
  const setPeriod = (days?: number) => { const today = new Date(); if (!days) { setStartDate(currentMonthStart()); setEndDate(currentMonthEnd()); return; } const start = new Date(today); start.setDate(today.getDate() - days + 1); setStartDate(iso(start)); setEndDate(iso(today)); };
  const exportPdf = async () => {
    const { jsPDF } = await import("jspdf"); const doc = new jsPDF({ orientation: "landscape", unit: "mm", format: "a4" }); const width = 297; const left = 15; const barWidth = 112;
    const period = `${readableDate(startDate)} a ${readableDate(endDate)}`; const sellerName = seller === "all" ? "Toda a equipe" : charts.data?.sellers.find(item => item.id === Number(seller))?.name || "Equipe selecionada";
    doc.setFillColor(29, 43, 42); doc.rect(0, 0, width, 32, "F"); doc.setTextColor(232, 208, 146); doc.setFont("times", "bold"); doc.setFontSize(22); doc.text("TSE Exclusive", left, 15); doc.setFont("helvetica", "normal"); doc.setFontSize(9); doc.text("RELATÓRIO EXECUTIVO COMERCIAL", left, 23);
    doc.setTextColor(29, 43, 42); doc.setFont("times", "bold"); doc.setFontSize(18); doc.text("Metas e funil de propostas", left, 45); doc.setFont("helvetica", "normal"); doc.setFontSize(10); doc.setTextColor(95, 104, 100); doc.text(`Período: ${period}  •  Equipe: ${sellerName}`, left, 53);
    const cards = [["Vendido no período", money(goalData.reduce((sum, item) => sum + item.currentAmount, 0))], ["Meta no período", money(goalData.reduce((sum, item) => sum + item.targetAmount, 0))], ["Pipeline em aberto", money(funnelData.filter(item => !["Ganhos", "Perdidos"].includes(item.label)).reduce((sum, item) => sum + item.amount, 0))]];
    cards.forEach(([label, value], index) => { const x = left + index * 86; doc.setFillColor(245, 243, 237); doc.roundedRect(x, 62, 78, 23, 3, 3, "F"); doc.setTextColor(177, 143, 75); doc.setFontSize(7); doc.text(label, x + 5, 69); doc.setTextColor(29, 43, 42); doc.setFont("times", "bold"); doc.setFontSize(15); doc.text(value, x + 5, 79); doc.setFont("helvetica", "normal"); });
    const drawBars = (title: string, data: { label: string; value: number; detail: string }[], x: number, y: number, color: [number, number, number]) => { doc.setTextColor(29, 43, 42); doc.setFont("times", "bold"); doc.setFontSize(15); doc.text(title, x, y); doc.setFont("helvetica", "normal"); const max = Math.max(...data.map(item => item.value), 1); data.slice(0, 6).forEach((item, index) => { const rowY = y + 10 + index * 15; const fill = Math.max(2, (item.value / max) * barWidth); doc.setTextColor(78, 88, 84); doc.setFontSize(8); doc.text(item.label, x, rowY); doc.setFillColor(236, 232, 222); doc.roundedRect(x, rowY + 3, barWidth, 5, 1, 1, "F"); doc.setFillColor(...color); doc.roundedRect(x, rowY + 3, fill, 5, 1, 1, "F"); doc.setTextColor(29, 43, 42); doc.text(item.detail, x + barWidth + 4, rowY + 7); }); };
    drawBars("Meta x vendido", goalData.map(item => ({ label: item.seller, value: item.currentAmount, detail: `${money(item.currentAmount)} de ${money(item.targetAmount)}` })), left, 102, [29, 91, 83]);
    drawBars("Funil de propostas", funnelData.map(item => ({ label: item.label, value: item.amount, detail: `${money(item.amount)} · ${item.count} oportunidade(s)` })), 158, 102, [199, 163, 90]);
    doc.setDrawColor(224, 218, 207); doc.line(left, 194, width - left, 194); doc.setTextColor(120, 125, 120); doc.setFontSize(8); doc.text("Gerado pelo TSE Exclusive para reunião de gestão.", left, 201); doc.text(`Atualizado em ${new Date().toLocaleString("pt-BR")}`, width - left, 201, { align: "right" }); doc.save(`tse-relatorio-comercial-${startDate}-${endDate}.pdf`);
  };
  return <div className="space-y-8"><PageHeader eyebrow="Operação em foco" title="Bom dia. Vamos colocar a casa em ordem." description="Acompanhe o pulso comercial, contratual e financeiro da operação sem abrir doze planilhas e perder a vontade de viver." />
    <Card className="rounded-[1.5rem] border-[#e8e3d9] shadow-sm"><CardContent className="p-5"><div className="flex flex-col gap-4 xl:flex-row xl:items-end xl:justify-between"><div className="grid gap-3 sm:grid-cols-2 xl:flex xl:items-end"><div className="grid gap-1.5"><Label className="text-[10px] font-bold uppercase tracking-[.11em] text-[#8a6b2d]">Início</Label><Input type="date" value={startDate} max={endDate} onChange={event => setStartDate(event.target.value)} className="w-full bg-[#faf8f3] xl:w-40" /></div><div className="grid gap-1.5"><Label className="text-[10px] font-bold uppercase tracking-[.11em] text-[#8a6b2d]">Fim</Label><Input type="date" value={endDate} min={startDate} onChange={event => setEndDate(event.target.value)} className="w-full bg-[#faf8f3] xl:w-40" /></div><div className="grid gap-1.5"><Label className="text-[10px] font-bold uppercase tracking-[.11em] text-[#8a6b2d]">Equipe de vendas</Label><Select value={seller} onValueChange={setSeller}><SelectTrigger className="w-full bg-[#faf8f3] xl:w-52"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="all">Toda a equipe</SelectItem>{charts.data?.sellers.map(item => <SelectItem key={item.id} value={String(item.id)}>{item.name}</SelectItem>)}</SelectContent></Select></div></div><div className="flex flex-wrap items-center gap-2"><Button size="sm" variant="outline" className="rounded-xl border-[#ded5c4]" onClick={() => setPeriod()}>Mês atual</Button><Button size="sm" variant="outline" className="rounded-xl border-[#ded5c4]" onClick={() => setPeriod(90)}>90 dias</Button><Button onClick={exportPdf} disabled={charts.isLoading} className="rounded-xl bg-[#1d2b2a] hover:bg-[#29413e]"><Download className="mr-2 h-4 w-4" />PDF gestão</Button></div></div><div className="mt-4 flex items-center gap-2 text-xs text-muted-foreground"><Filter className="h-3.5 w-3.5 text-[#b18f4b]" />Dados filtrados de {readableDate(startDate)} a {readableDate(endDate)} · atualização automática a cada 30 segundos.</div></CardContent></Card>
    <section className="grid gap-4 md:grid-cols-2 xl:grid-cols-4"><MetricCard label="Contratos ativos" value={loading ? "—" : summary.data?.activeContracts ?? 0} detail="Base contratual vigente" icon={FileCheck2} tone="dark" /><MetricCard label="Inadimplência" value={loading ? "—" : money(summary.data?.overdueAmount)} detail="Parcelas que exigem ação" icon={TriangleAlert} tone="gold" /><MetricCard label="Ocupação do mês" value={loading ? "—" : `${summary.data?.occupancy ?? 0}%`} detail="Uso das unidades ativas" icon={CalendarCheck2} tone="sage" /><MetricCard label="Tarefas pendentes" value={loading ? "—" : summary.data?.pendingTasks ?? 0} detail="Follow-ups e rotinas abertas" icon={UsersRound} tone="cream" /></section>
    <section className="grid gap-5 xl:grid-cols-2"><Card className="rounded-[1.5rem] border-[#e8e3d9] shadow-sm"><CardHeader className="flex flex-row items-start justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[.14em] text-[#b18f4b]">Meta comercial</p><CardTitle className="mt-1 font-serif text-2xl text-[#1d2b2a]">Valor vendido x meta</CardTitle><p className="mt-1 text-xs text-muted-foreground">Recorte aplicado: {readableDate(startDate)} a {readableDate(endDate)}.</p></div><Target className="h-5 w-5 text-[#b18f4b]" /></CardHeader><CardContent>{goalData.length ? <div className="h-[290px]"><ResponsiveContainer width="100%" height="100%"><BarChart data={goalData} margin={{ top: 8, right: 8, left: 0, bottom: 4 }}><CartesianGrid vertical={false} stroke="#eee9df" /><XAxis dataKey="seller" tickLine={false} axisLine={false} fontSize={11} /><YAxis tickFormatter={currencyAxis} tickLine={false} axisLine={false} fontSize={10} width={72} /><Tooltip formatter={(value: number) => money(value)} contentStyle={{ borderRadius: 12, borderColor: "#e8e3d9" }} /><Legend wrapperStyle={{ fontSize: 11 }} /><Bar dataKey="targetAmount" name="Meta" fill="#d8d1bf" radius={[6, 6, 0, 0]} /><Bar dataKey="currentAmount" name="Vendido" fill="#1d5b53" radius={[6, 6, 0, 0]} /></BarChart></ResponsiveContainer></div> : <div className="flex h-[290px] items-center justify-center rounded-xl bg-[#faf8f3] text-center text-sm text-muted-foreground">Defina uma meta no período selecionado e esse gráfico deixa de ser enfeite de reunião.</div>}</CardContent></Card><Card className="rounded-[1.5rem] border-[#e8e3d9] shadow-sm"><CardHeader className="flex flex-row items-start justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[.14em] text-[#b18f4b]">Radar de proposta</p><CardTitle className="mt-1 font-serif text-2xl text-[#1d2b2a]">Funil comercial em valor</CardTitle><p className="mt-1 text-xs text-muted-foreground">Cada barra representa o valor estimado por estágio no recorte.</p></div><BadgeDollarSign className="h-5 w-5 text-[#b18f4b]" /></CardHeader><CardContent>{funnelData.some(item => item.count) ? <div className="h-[290px]"><ResponsiveContainer width="100%" height="100%"><BarChart data={funnelData} layout="vertical" margin={{ top: 8, right: 20, left: 4, bottom: 4 }}><CartesianGrid horizontal={false} stroke="#eee9df" /><XAxis type="number" tickFormatter={currencyAxis} tickLine={false} axisLine={false} fontSize={10} /><YAxis type="category" dataKey="label" tickLine={false} axisLine={false} fontSize={11} width={85} /><Tooltip formatter={(value: number) => money(value)} contentStyle={{ borderRadius: 12, borderColor: "#e8e3d9" }} /><Bar dataKey="amount" name="Valor em funil" fill="#c7a35a" radius={[0, 7, 7, 0]} /></BarChart></ResponsiveContainer></div> : <div className="flex h-[290px] items-center justify-center rounded-xl bg-[#faf8f3] text-center text-sm text-muted-foreground">Esse recorte não tem proposta. Ajusta a data ou manda o comercial alimentar o funil, porra.</div>}</CardContent></Card></section>
    <section className="grid gap-5 xl:grid-cols-[1.55fr_1fr]"><Card className="rounded-[1.5rem] border-[#e8e3d9] shadow-sm"><CardHeader className="flex flex-row items-center justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[.14em] text-[#b18f4b]">Contratos recentes</p><CardTitle className="mt-1 font-serif text-2xl text-[#1d2b2a]">Pasta contratual</CardTitle></div><CalendarCheck2 className="h-5 w-5 text-[#b18f4b]" /></CardHeader><CardContent className="space-y-3">{contracts.data?.slice(0, 5).map(({ contract, customerName }) => <div key={contract.id} className="flex items-center justify-between gap-3 rounded-xl bg-[#faf8f3] px-4 py-3"><div className="min-w-0"><p className="truncate text-sm font-semibold text-[#1d2b2a]">{contract.number} <span className="font-normal text-muted-foreground">· {customerName}</span></p><p className="mt-1 text-xs text-muted-foreground">Criado {dateLabel(contract.createdAt)} · {money(contract.totalAmount)}</p></div><StatusPill value={contract.status} /></div>)}{!contracts.data?.length && !loading ? <p className="rounded-xl bg-[#faf8f3] p-5 text-sm text-muted-foreground">Ainda não há contratos. Comece pela área de formalização.</p> : null}</CardContent></Card><Card className="rounded-[1.5rem] border-[#e8e3d9] bg-[#1d2b2a] text-white shadow-sm"><CardHeader><p className="text-[10px] font-bold uppercase tracking-[.14em] text-[#d6b774]">Radar comercial</p><CardTitle className="mt-1 font-serif text-2xl text-white">Oportunidades em curso</CardTitle></CardHeader><CardContent className="space-y-3">{pipeline.data?.filter(item => !["won", "lost"].includes(item.opportunity.stage)).slice(0, 5).map(({ opportunity, customerName }) => <div key={opportunity.id} className="rounded-xl border border-white/10 bg-white/5 p-3"><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-semibold">{opportunity.title}</p><p className="mt-1 text-xs text-white/55">{customerName} · {money(opportunity.expectedAmount)}</p></div><span className="text-xs font-bold text-[#d6b774]">{opportunity.probability}%</span></div></div>)}{!pipeline.data?.filter(item => !["won", "lost"].includes(item.opportunity.stage)).length && !loading ? <p className="rounded-xl border border-white/10 bg-white/5 p-5 text-sm text-white/60">Nenhuma oportunidade em andamento. Bora alimentar esse funil.</p> : null}</CardContent></Card></section></div>;
}
